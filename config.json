{
    "user": "root",
    "password": "passwordMuySecreto69-",
    "port": 3001,
    "firma": "dormirLaSiestaHaceBien",
    "queryPostUser": "INSERT INTO usuarios (usuario,nombre,mail,tel,direccionEnvio,urlDireccionEnvio,hysPass,salt,activo,administ ) values(:usuario,:nombre,:mail,:tel,:direccionEnvio,:urlDireccionEnvio,:hysPass,:salt,:activo,:administ)",
    "queryDbUser": "SELECT * FROM delilah.usuarios WHERE usuario LIKE BINARY :usuario",
    "queryDbUserId": "SELECT id FROM delilah.usuarios WHERE usuario LIKE BINARY :usuario",
    "queryProduct": "SELECT * FROM delilah.productos WHERE nombre LIKE BINARY :nombre",
    "queryAllProducts": "SELECT * FROM delilah.productos",
    "queryPostProduct": "INSERT INTO productos (nombre,descripcion,precio,categoria,img_url) values(:nombre,:descripcion,:precio,:categoria,:img_url)",
    "deleteProduct": "UPDATE productos SET activo = 0 WHERE id = :id"
}
